{% extends 'base.html.twig' %}

{% block body %}
    <h1>Creation de box</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-success" value="Valider" />
    <a href="{{ path('box_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left"></i> Retour a la liste</a>
    {{ form_end(form) }}



{% endblock %}
